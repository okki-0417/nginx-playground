{
  "name": "server",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run --watch server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@fastify/static": "^9.0.0",
    "fastify": "^5.6.2"
  }
}
